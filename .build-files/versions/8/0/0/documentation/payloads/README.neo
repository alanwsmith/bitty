-- title

Payload Readme

-- list

- The `valid-element.xml` has that extension instead
of `.html` to prevent the local testing server
I have from injecting a reload script. 

Everything is processed as text so there's no
practical impact. 
