### Version 5.1.0-rc5

DATE TBD

- TODO: Set up `this.api.trigger(SIGNAL)` to send event
that bubbles up to the document root

- TODO: Set up `this.api.forward(EVENT, SIGNAL)` to send event
that bubbles up to the document root

- TODO: Set up so bittyids are added to everything on 
connectedcallback

- TODO: Condsider: adding UUIDs to everything
on init. And on makeHTML, getHTML, makeElement, 
getElement when those fire. 
