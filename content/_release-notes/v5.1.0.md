Version: 5.1.0

DATE TBD

- TODO: Added `bittyReady()` call that
works like `bittyInit()` but happens
after `this.runSendFromComponent();`

- TODO: If `bittyReady()` is defined
as async (i.e. `async bittyReady()`) it's
called with `await`


