### Version: 6.0.0-rc3

DATE TBD

- TODO: Added `event.target.stringValue` the
returns the `event.target.value` as a string. 
(this is effectively an alias that's only here
to keep the mental model with `.intValue` and
`.floatValue` consistent)

- TODO: Added `event.target.intValue` the
returns the `event.target.value` as an
integer. 

- TODO: Added `event.target.floatValue` the
returns the `event.target.value` as an
float. 

- TODO: Added `event.target.getString(KEY)`

- TODO: Added `event.target.getInt(KEy)`

- TODO: Added `event.target.getFloat(KEY)`

- TODO: Added `event.sender.getString(KEY)`

- TODO: Added `event.sender.getInt(KEY)`

- TODO: Added `event.sender.getFloat(KEY)`

- TODO: Added `el.matchString(KEY)` which 
_does not_ travel up the DOM. It does a 
direct comparison of (`event.target.dataset.KEY` 
or `event.sender.dataset.KEY`) and
`el.dataset.KEY`. 

- TODO: Consider `el.matchTargetString/Int/Float(KEY)`
and `el.matchSenderString/Int/Float(KEY)`
that would work like `el.matchString/Int/Float(KEY)`
but would look at `event.target` and `event.sender`
separately. 

- Remove setting `style="display: block;"`.
Leaving this as a decision at implementation time. 


