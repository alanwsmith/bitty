### Version: 7.0.0-rc1

- This update is a set of API refinements to 
make things more obvious and consistent. 

- TODO: The sender is still added to Events, but
no extra helper methods are added to the `ev.target` or
`ev.sender` elements. The primary helpers have
been moved directly to the incoming element (`el`
in the examples). The general idea is to be able
to use the `el` in the methods for the majority 
of the work. (i.e. you don't have to remember
if things are in the event or the element. They'll
just be in the element for standard usage)

- Moved `el.getString(KEY)` to `el.stringData(KEY)` 
- Moved `el.getInt(KEY)` to `el.intData(KEY)` 
- Moved `el.getFloat(KEY)` to `el.floatData(KEY)` 

- TODO: Removed `ev.target.getString(KEY)` 
- Added: `el.targetStringData(KEY)`

- TODO: Removed `ev.target.getInt(KEY)`
- Added: `el.targetIntData(KEY)`

- TODO: Removed `ev.target.getFloat(KEY)` 
- Added: `el.targetFloatData(KEY)`

- TODO: Removed `ev.sender.getString(KEY)`
- TODO: Added: `el.senderStringData(KEY)`

- TODO: Removed `ev.sender.getInt(KEY)` 
- TODO: Added: `el.senderIntData(KEY)`

- TODO: Removed `ev.sender.getFloat(KEY)`
- TODO: Added: `el.senderFloatData(KEY)`

- TODO: Renamed `el.bittyId` to `el.id` to be consistent with the other id strings. 
- TODO: Added `el.targetId`
- TODO: Added `el.senderId`

- TODO: Renamed `el.matchTarget(KEY)` to `el.matchTargetData(KEY)`
- TODO: Renamed `el.matchSender(KEY)` to `el.matchSenderData(KEY)`

