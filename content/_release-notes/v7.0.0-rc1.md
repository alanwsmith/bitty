### Version: 7.0.0-rc1

- TODO: Renamed `el.getString(KEY)` to `el.stringData(KEY)` 

- TODO: Renamed `el.getInt(KEY)` to `el.intData(KEY)` 

- TODO: Renamed `el.getFloat(KEY)` to `el.floatData(KEY)` 

- TODO: Renamed `el.matchTarget(KEY)` to `el.matchTargetData(KEY)`

- TODO: Renamed `el.matchSender(KEY)` to `el.matchSenderData(KEY)`


