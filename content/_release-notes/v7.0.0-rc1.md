### Version: 7.0.0-rc1

- Renamed `el.getString(KEY)` to `el.stringData(KEY)` 

- Renamed `el.getInt(KEY)` to `el.intData(KEY)` 

- Renamed `el.getFloat(KEY)` to `el.floatData(KEY)` 

- TODO: Renamed `el.matchTarget(KEY)` to `el.matchTargetData(KEY)`

- TODO: Renamed `el.matchSender(KEY)` to `el.matchSenderData(KEY)`


