### Version: 7.0.0-rc2

This change primarily moves `.target` and
`.sender` items from `el` to `ev`.
And moves the sending element into
`ev.sender` with extra the features added.

- Renamed `ev.val` to `ev.value`

- Renamed `ev.valInt` to `ev.intValue`

- Renamed `ev.valFloat` to `ev.floatValue`

- Renamed `ev.ds` to `ev.param` 

- Renamed `ev.dsInt` to `ev.paramAsInt` 

- Renamed `ev.dsFloat` to `ev.paramAsFloat` 

- Added `ev.sender.value`

- Added `ev.sender.valueAsInt`

- Added `ev.sender.valueAsFloat`

- Moved `el.targetBittyId` to `ev.bittyId`

- Removed `el.targetDs()` in favor of existing
`ev.param()`

- Removed `el.targetDsInt()` in favor of existing
`ev.paramAsInt()`

- Removed `el.targetDsFloat()` in favor of existing
`ev.paramAsFloat()`

- TODO: Moved `el.senderBittyId` to `ev.sender.bittyId`

- TODO: Removed `el.senderDs()` in favor of existing
`ev.sender.param()`

- TODO: Removed `el.senderDsInt()` in favor of existing
`ev.sender.paramAsInt()`

- TODO: Removed `el.senderDsFloat()` in favor of existing
`ev.sender.paramAsFloat()`

- TODO: Removed `el.val` in favor of the standard
`el.value`

- TODO: Renamed `el.valInt` to `el.valueAsInt`

- TODO: Renamed `el.valFloat` to `el.valueAsFloat`

- TODO: Removed `el.targetVal` in favor of 
`ev.value`

- TODO: Removed `el.targetValInt` in favor of 
`ev.valueAsInt`

- TODO: Removed `el.targetValFloat` in favor of 
`ev.valueAsFloat`

- TODO: Removed `el.senderVal` in favor of 
`ev.sender.value`

- TODO: Removed `el.senderValInt` in favor of 
`ev.sender.valueAsInt`

- TODO: Removed `el.senderValueFloat` in favor of 
`ev.sender.valueAsFloat`

- Renamed `el.matchTargetDs` to `el.matchTarget`

- Renamed `el.matchSenderDs` to `el.matchSender`

