### Version: 7.0.0-rc2

This change moves `.target` and
`.sender` items from `el` to `ev`.

- TODO: Renamed `ev.val` to `ev.value`

- TODO: Renamed `ev.valInt` to `ev.intValue`

- TODO: Renamed `ev.valFloat` to `ev.floatValue`

- TODO: Added `ev.senderValue`

- TODO: Added `ev.senderIntValue`

- TODO: Added `ev.senderFloatValue`

- TODO: Moved `el.targetBittyId` to `ev.bittyId`

- TODO: Removed `el.targetDs()` in favor of existing
`ev.ds()`

- TODO: Removed `el.targetDsInt()` in favor of existing
`ev.dsInt()`

- TODO: Removed `el.targetDsFloat()` in favor of existing
`ev.dsFloat()`

- TODO: Moved `el.senderBittyId` to `ev.senderBittyId`

- TODO: Removed `el.senderDs()` in favor of existing
`ev.senderDs()`

- TODO: Removed `el.senderDsInt()` in favor of existing
`ev.senderDsInt()`

- TODO: Removed `el.senderDsFloat()` in favor of existing
`ev.senderDsFloat()`

- TODO: Removed `el.val` in favor of the standard
`el.value`

- TODO: Renamed `el.valInt` to `el.valueInt`

- TODO: Renamed `el.valFloat` to `el.valueFloat`

- TODO: Removed `el.targetVal` in favor of 
`ev.value`

- TODO: Removed `el.targetValInt` in favor of 
`ev.valueInt`

- TODO: Removed `el.targetValFloat` in favor of 
`ev.valueFloat`

- TODO: Removed `el.senderVal` in favor of 
`ev.senderValue`

- TODO: Removed `el.senderValInt` in favor of 
`ev.senderIntValue`

- TODO: Removed `el.senderValueFloat` in favor of 
`ev.senderFloatValue`

