export default class {
  alfa(_event, element) {
    element.innerHTML = Date.now();
  }

  bravo(_event, element) {
    element.innerHTML = Date.now();
  }

  charlie(_event, element) {
    element.innerHTML = Date.now();
  }
}
