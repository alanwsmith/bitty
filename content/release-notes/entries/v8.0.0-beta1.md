### Version 8.0.0-beta-1

DATE TBD

NOTE: These are work in progress notes
for the next version of bitty. 
Check the `Version 7.0.0` notes below
for the current version. 

- Added automatic loading of `<template>` tags
in the source page. If the tag has an 
`id` attribute it's used as the key when
called with `this.api.template(ID)`. 
The returned values are strings converted
from the `.innerHTML`.


