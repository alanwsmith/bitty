export const b = {};

export function $_SIGNAL_(ev, sender, el) {
  // const result = b.load("intentionally-missing-$_SIGNAL_");
  // if (result === undefined) {
  //   el.innerHTML = b.time();
  // }
}
