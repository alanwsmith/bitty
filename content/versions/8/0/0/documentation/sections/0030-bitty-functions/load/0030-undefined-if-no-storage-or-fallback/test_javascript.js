export const bitty = {};

export function signal_D6B1F(ev, sender, el) {
  const result = bitty.load("intentionally-missing-signal_D6B1F");
  if (result === undefined) {
    el.innerHTML = bitty.time();
  }
}
