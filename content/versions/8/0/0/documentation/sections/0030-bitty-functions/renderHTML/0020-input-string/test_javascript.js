export const bitty = {};

export function signal_8988F(ev, sender, el) {
  const html = bitty.renderHTML("<div>signal_8988F</div");
  const result = html.firstChild.innerHTML;
  if (result === "signal_8988F") {
    el.innerHTML = bitty.localTimestamp();
  }
}
