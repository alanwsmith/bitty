export const b = {};

export function signal_F847B_v1(ev, sender, el) {
  const result = b.restore("intentionally-missing-signal_F847B_v1");
  if (result === undefined) {
    el.innerHTML = b.time();
  }
}
