export const b = {};

export function signal_24CEF_v1(ev, sender, el) {
  const check = b.svg["svg_signal_24CEF_v1"].querySelector("text");
  if (check.innerHTML === "svg") {
    el.innerHTML = b.time();
  }
}
