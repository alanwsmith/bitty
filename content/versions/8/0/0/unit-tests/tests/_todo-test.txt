### TODO Tests

- Add test for data-int doing `this.prepElements(event, event.el);`.
The code is in place, just not a test yet. 

- Add tests for the `this.api.getTEXT()` element, documnet
fragment, and array find replace subs that
mimic `this.api.makeTEXT()`. 

- Add tests for the different `this.api.getTHING()`
that mimic `this.api.makeTHING()`

- Add test for preventDefault for form submissions


