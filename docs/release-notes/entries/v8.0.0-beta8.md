## Version 8.0.0-beta8

DATE: TBD

### TODO

- TODO: Updated IndexedDB data for `savePageData()`
and `loadPageData()` to have a single top level
database for all pages instead of one DB per page. 

- TODO: Prevent `input type="serach"` from sending
a signal when it gains or loses focus (i.e. it
only sends signals when the content of the 
field changes).
