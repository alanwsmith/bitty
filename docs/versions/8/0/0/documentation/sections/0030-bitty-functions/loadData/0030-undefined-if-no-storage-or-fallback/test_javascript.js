export const bitty = {};

export function signal_81255(ev, sender, el) {
  const result = bitty.loadData("intentionally-missing-signal_81255");
  if (result === undefined) {
    el.innerHTML = bitty.time();
  }
}
