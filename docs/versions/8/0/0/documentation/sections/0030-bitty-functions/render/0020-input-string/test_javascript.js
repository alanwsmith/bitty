export const bitty = {};

export function signal_736E9(ev, sender, el) {
  const html = bitty.render("<div>signal_736E9</div");
  const result = html.firstChild.innerHTML;
  if (result === "signal_736E9") {
    el.innerHTML = bitty.time();
  }
}
