export const bitty = {};

export function signal_736E9_alfa(ev, sender, el) {
  const html = bitty.render("<div>signal_736E9_alfa</div");
  const result = html.firstChild.innerHTML;
  if (result === "signal_736E9_alfa") {
    el.innerHTML = bitty.time();
  }
}
